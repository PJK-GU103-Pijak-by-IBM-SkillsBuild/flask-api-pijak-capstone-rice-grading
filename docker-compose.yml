services:
  app:
    image: arykurnia/api-rice-grading-flask:latest        # image yang sudah ada
    container_name: flask-rice-grading
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    restart: unless-stopped
